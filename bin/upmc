#!/usr/bin/python
import sys
# For some reason on the netbooted SOEs I'm using /usr/share/pyshared is not included in the default Python path.
if "/usr/share/pyshared" not in sys.path:
  sys.path.append("/usr/share/pyshared")
import upmc
upmc.main(sys.argv)
